/** Canonical app routes - use instead of hardcoding paths in components. */

/** People directory (search, filters, connect). */
export const PEOPLE_DIRECTORY_PATH = '/people'

/** Curated discovery dashboard (sections link to real directories). */
export const EXPLORE_DASHBOARD_PATH = '/explore'

/** @deprecated Use PEOPLE_DIRECTORY_PATH - `/discovery` redirects for bookmarks. */
export const LEGACY_DISCOVERY_PATH = '/discovery'
