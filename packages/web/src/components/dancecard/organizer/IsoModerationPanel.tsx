/** @deprecated Use `IsoBoardModerationPanel` — this is convention ISO-board moderation, not platform ISO. */
export { IsoBoardModerationPanel, IsoBoardModerationPanel as IsoModerationPanel } from './IsoBoardModerationPanel'
