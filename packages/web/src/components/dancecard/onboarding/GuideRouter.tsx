import type { OrganizerTab } from '@/components/dancecard/organizer/shell/organizerNavConfig'

/** Onboarding guide overlay - stub until Kink Social wires guide flows. */
export function GuideRouter(_props: {
  eventSlug: string
  onSwitchTab: (tab: OrganizerTab) => void
}) {
  return null
}
