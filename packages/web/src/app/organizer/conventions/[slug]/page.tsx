import OrganizerConventionManageRedirect from './OrganizerConventionManageRedirect'

/** Convention program ops embedded in organizer context via `?organizerOrg=` on the convention page. */
export default function OrganizerConventionManagePage() {
  return <OrganizerConventionManageRedirect />
}
